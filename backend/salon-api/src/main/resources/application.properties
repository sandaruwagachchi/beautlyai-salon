# Default to the local profile for manual runs.
# Docker overrides still work via SPRING_PROFILES_ACTIVE=docker.
spring.profiles.default=local

# Base settings shared by all profiles.
spring.application.name=salon-api
